export const getCurrentAudioIndex = (state) => {
    return state.audio.currentAudioIndex;
}

export const getSongs = (state) => {
    return state.audio.songs;
}

export const getSongsType = (state) => {
    return state.audio.songsType;
}

export const getSongsArr = (state) => {
    return state.audio.songsArr;
}

export const getIsPlayId = (state) => {
    return state.audio.isPlayId;
}